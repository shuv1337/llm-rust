//! Placeholder embeddings crate.
//!
//! Ultimately this crate will handle SQLite-backed embedding storage and
//! similarity search. For now it provides a minimal stub API.

/// Returns the number of embeddings currently stored (always zero for now).
pub fn embedding_count() -> usize {
    0
}
